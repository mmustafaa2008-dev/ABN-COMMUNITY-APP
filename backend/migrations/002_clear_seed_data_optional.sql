-- Optional: run in Supabase SQL Editor to wipe seeded demo listings/jobs
-- Keeps table structure intact; auth demo accounts remain in backend in-memory store only.

-- delete from public.jobs_board;
-- delete from public.profiles_directory;

-- Verify clean state:
-- select count(*) from public.profiles_directory;
-- select count(*) from public.jobs_board;
