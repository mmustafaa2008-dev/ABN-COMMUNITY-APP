# Railway — set Root Directory to "backend" when importing the repo
# Dashboard → Variables: copy from backend/.env.example (Supabase, JWT_SECRET, etc.)

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
